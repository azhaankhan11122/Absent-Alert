{
  "name": "absent-alert",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack student attendance management with USB Android SMS gateway queue.",
  "scripts": {
    "dev": "concurrently \"npm:server:dev\" \"npm:client:dev\" \"npm:whatsapp:dev\"",
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "whatsapp:dev": "npm --prefix whatsapp-service start",
    "server:start": "npm --prefix server start",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install && npm --prefix whatsapp-service install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
